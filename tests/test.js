const { describe, it } = require('../help/rewrite')
const { clickNavigation, dataHandle } = require('../help/flow')

describe('get KYC title title', User = 'KYC', () => {
    it('start get...', async () => {
        await clickNavigation('Data Entry', 'SVIP MAIN ID')
        // await dataHandle()
        // await expect(page.title()).resolves.toMatch('TNG KYC Website')
    })
})